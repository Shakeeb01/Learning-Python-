# Enumerate functio is a built in function in python that allows you to loop over a 

# Enumerate give us the index while iterating over the sequence data while looping.
name = "shakeeb"
for  index,char in enumerate(name,start=0): # this start argument will specify the starting index.
    print(index,char)